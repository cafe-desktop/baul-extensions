### baul-extensions 1.24.1

  * Translations update
  * sendto: Fix evolution command for sending an email
  * build: silent build warnings for distcheck

### baul-extensions 1.24.0

  * Translations update
  * baul-sendto: rewrite baul-sendto.ui from scratch fixing deprecations
  * baul-sendto-command: add .7z,.tar.xz to packed type drop-down list
  * baul-wallpaper-extension: remove unused pointer 'GError *err'
  * baul-wallpaper-extension: fix memory leak
  * image-converter: i18n select an angle drop-down list
  * image-converter: rewrite baul-image-resize.ui from scratch fixing deprecations
  * sendto: value stored to 'file' is never read
  * configure: get quieter output from make

### baul-extensions 1.23.1

  * Translations update
  * migrate from intltool to gettext
  * baul-image-resizer: avoid 'g_type_class_add_private'
  * baul-image-rotator: avoid 'g_type_class_add_private'
  * i18n baul extension copyright
  * Build either with gupnp version 1.0 or 1.2.
  * image-converter: don't use non-existing icons in context menu

### baul-extensions 1.23.0

  * Translations update
  * Remove trailing whitespaces
  * Strings resistance
  * Change url project's website
  * [ci] Add cppcheck html report
  * [ci] Enable Clang Static Analyzer

### baul-extensions 1.22.0

  * Translations update
  * Initialize Travis CI support
  * require baul >= 1.21.3

### baul-extensions 1.21.0

  * Translations update
  * disable deprecation warnings for distcheck
  * Make baul-open-terminal configurable (Depends on baul GitHub PR baul#1050)
    
### baul-extensions 1.20.0

  * Translations update
  * require CTK+ 3.22 and GLib 2.50
  * xattr-tags: use better English in extension description
  * po: add missing translatable strings from xattrs extension
  * xattr-tags: fix extension description

### baul-extensions 1.19.0

  * Translations update
  * open-terminal: don't use excessive string duplication
  * avoid deprecated gdk_screen_make_display_name
  * sendto burn: avoid deprecated ctk_show_uri
  * xattr-tags: remove hack, it's not needed now
  * xattr-tags: fix typo causing undefined reference
  * fix the libbaul_gksu_la_LDFLAGS variable declaration
  * xattr-tags: add missing newline to startup message
  * xattr-tags: unescaping stored attributes.
  * README: Added development info.

### baul-extensions 1.18.0

  * NEWS: use consistent, project wide, markdown-like formatting to make
    generating release announcements easier
  * Build: require baul >= 1.17.1 and libcafe-desktop >= 1.17.0
  * Move to CTK+3 (require CTK+ >= 3.14), drop CTK+2 code and --with-ctk
    build option
  * Add xattr-tags extension to show xattr tags in a new column
  * Translations update

### baul-extensions 1.16.0

  * open-terminal: fix opening terminal in remote folders with single
    or double quote in their names
  * open-terminal: some code cleanups
  * Translations update

### baul-extensions 1.14.1

  * Translations update
  * Make distcheck use currently selected CTK+ version

### baul-extensions 1.14.0

  * Drop CafeConf migration scripts
  * CTK+3: some deprecation fixes
  * Some more fixes and cleanups
  * Translations update
  * Add Changelog generation

### baul-extensions 1.12.0

  * Some string fixes
  * Translations update

### baul-extensions 1.10.1

  * open-terminal: fixed bugs with non-standard ssh ports

### baul-extensions 1.10.0

  * wallpaper: Added new 'Set as wallpaper' extension
  * sendto: re-introduce the option to select plugins to build

### baul-extensions 1.8.0

  * New release merging all current official CAFE baul extensions in a single
    package.
